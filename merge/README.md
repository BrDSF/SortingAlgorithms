-> Time complexity for all case is nlog(n), the lists are divided using max log(n) parts,
and the merging of the sublists take O(n) time.

-> Space complexity is O(n), because the algorithm need more memory space allocated in execution time
